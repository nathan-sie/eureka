spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/nate777/springcloud-config.git
          username: nate777
          password: limingyang891120
          label: master

server:
  port: 7755